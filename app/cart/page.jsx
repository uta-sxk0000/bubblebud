import { CartPage } from "@/components/customer-pages";

export const metadata = {
  title: "Cart",
  description: "Review your BubbleBud cart and continue to secure checkout.",
};

export default function Page() {
  return <CartPage />;
}
