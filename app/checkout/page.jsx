import { CartPage } from "@/components/customer-pages";

export const metadata = {
  title: "Checkout",
  description: "Start BubbleBud secure checkout.",
};

export default function Page() {
  return <CartPage />;
}
