import { AccountPage } from "@/components/pages";

export const metadata = {
  title: "Account",
  description: "Log in, create an account, view BubbleBud orders, manage wishlist items, and save addresses.",
};

export default function Page() {
  return <AccountPage />;
}
